services:
  build:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    volumes:
      - .:/workspace
    command: >
      sh -lc "cmake -S . -B build/cmake-build &&
      cmake --build build/cmake-build"
